dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
-- wsl --unregister Ubuntu
wsl --install -d Ubuntu
wsl --set-default-version 2
wsl -l -v

#open ubuntu terminal
sudo apt-get update
sudo apt-get install lua5.1
sudo apt-get install luarocks
#install luasocket
sudo luarocks install luasocket
#to install luasec
sudo apt update
sudo apt install openssl libssl-dev
sudo apt install libssl-dev
openssl version
dpkg -L libssl-dev | grep include
sudo apt update
sudo apt install libssl-dev
ls /usr/include/openssl/ssl.h
sudo luarocks install luasec OPENSSL_INCDIR=/usr/include OPENSSL_LIBDIR=/usr/lib/x86_64-linux-gnu
sudo luarocks install dkjson
#mysql configuration
sudo apt install mysql-server
sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf(change port)
sudo service mysql start
sudo service mysql status
sudo service --status-all
sudo apt install net-tools
netstat -tlnp
sudo mysql_secure_installation
#login as admin
sudo mysql
#after that set password
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'newpassword';
mysql -u root  -p
create database ecommercedb;
use ecommercedb;
create table Product(ProductId int primary key, Name varchar(50), Cost int);
sudo apt-get install lua-sql-mysql

