sudo apt update
sudo apt install libbson-dev libmongoc-dev
ls /usr/include/libbson-1.0/bson/bson.h
sudo apt install cmake build-essential pkg-config libssl-dev
git clone https://github.com/mongodb/mongo-c-driver.git
cd mongo-c-driver
git checkout 1.24.4  # Use a stable version
mkdir cmake-build
cd cmake-build
cmake -DENABLE_AUTOMATIC_INIT_AND_CLEANUP=OFF ..
make -j$(nproc)
sudo make install
sudo luarocks install lua-mongo LIBBSON_INCDIR=/usr/local/include/libbson-1.0 LIBMONGOC_INCDIR=/usr/local/include/libmongoc-1.0 LIBBSON_LIBDIR=/usr/local/lib LIBMONGOC_LIBDIR=/usr/local/lib
#install mongodb
#if ubuntu 24
https://www.cherryservers.com/blog/install-mongodb-ubuntu-2404
for mongo installation

# Import MongoDB GPG key
curl -fsSL https://pgp.mongodb.com/server-7.0.asc | \
  sudo gpg -o /usr/share/keyrings/mongodb-server-7.0.gpg --dearmor
# Add MongoDB repository (for Ubuntu 20.04/22.04/24.04)
echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-7.0.gpg ] https://repo.mongodb.org/apt/ubuntu $(lsb_release -sc)/mongodb-org/7.0 multiverse" | \
  sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list
# Update package list
sudo apt update
# Install MongoDB server, shell, tools
sudo apt install -y mongodb-org
sudo systemctl start mongod
sudo systemctl status mongod
mongosh


